{
  "name": "iris",
  "version": "1.0.0",
  "description": "CDN with image crop and resize functionality built w/ Serverless",
  "main": "handler.js",
  "scripts": {
    "test": "nyc --check-coverage --lines 100 --functions 100 --branches 100 --statements 100 --reporter=text mocha **/*.test.js",
    "start": "npm run rebuild && sls offline start --stage local",
    "rebuild": "rm -r src/node_modules && cd src && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/britannica/iris.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/britannica/iris/issues"
  },
  "homepage": "https://github.com/britannica/iris#readme",
  "dependencies": {
    "winston": "^2.4.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.0.0",
    "nyc": "^11.4.1",
    "serverless-offline": "^3.16.0"
  }
}
